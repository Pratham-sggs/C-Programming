#include <stdio.h>
int main()
{
int m=40,n=20, A, O, XOR ;

A= (m&n);
O = (m|n);
XOR = (m^n);
printf("AND oprator value = %d\n",A );
printf("XOR oprator value = %d\n",XOR );
printf("OR oprator value = %d\n",O );
}
//........
